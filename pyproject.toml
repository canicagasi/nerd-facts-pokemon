[project]
name = "nerd-facts-pokemon"
version = "0.1.0"
description = "Pokemon data platform — dlt + dbt + Evidence on DuckDB"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dlt[duckdb]>=1.0.0",
    "dbt-duckdb>=1.9.0",
    "requests>=2.31.0",
]
